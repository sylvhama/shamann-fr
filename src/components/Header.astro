---
interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<header class="text-center font-mono">
  <h1 class="pb-4 text-[2rem] font-bold leading-none">{title}</h1>
  <h2 class="text-[1rem] leading-none">{subtitle}</h2>
</header>
